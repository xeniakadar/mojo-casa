<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <title><%= title %></title>
</head>
<body>
  <nav class="nav">
    <a href="/ " class="nav-link ">Home</a>
    <div class="nav">
      <a class="nav-link" href="/sign-up">Sign up</a>
      <a class="nav-link" href="/log-in">Log in</a>
    </div>
  </nav>
  </nav>
  <div class="container form-container">
    <a href="/profile" class="btn-submit back">Back</a>
    <h1><span>Update Profile</span></h1>
    <p class="warning">Even if you are not changing your password, you must still enter your password to update your profile</p>
    <form action="/update-profile" method="POST">
      <label for="email">Email</label>
      <input name="email" placeholder="email" type="text" value="<%= user.email %>"/>
      <label for="username">Username</label>
      <input name="username" placeholder="username" type="text" value="<%= user.username %>"/>
      <label for="password">Password</label>
      <input name="password" type="password"/>
      <label for="avatar">Choose an avatar:</label>
      <select name="avatar" id="avatar">
        <option value="ken.jpeg">Ken</option>
        <option value="ken1.jpeg">Ken Again</option>
        <option value="ken2.jpeg">A different Ken</option>
        <option value="ken3.jpeg">Just Ken</option>
        <option value="ken4.jpeg">Ken Again?</option>
        <option value="ken5.jpeg">Surprise Ken</option>
      </select>
      <button class="btn-submit" type="submit">Update</button>
      <% if (errors) { %>
        <ul>
          <% for (let i = 0; i < errors.length; i++) { %>
            <li><%= errors[i].msg %></li>
          <% } %>
        </ul>
      <% } %>
    </form>
  </div>
</body>
</html>
